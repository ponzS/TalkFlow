manifest: ./lzc-manifest.yml
contentdir: ./dist
icon: ./lzc-icon.png
pkgout: ./
buildscript: |
  pnpm build:web
  mkdir -p dist/scripts dist/relay
  cp -R gun-vue-call-relay/* dist/relay/
  rm -rf dist/relay/node_modules
  cp scripts/start-relay.sh dist/scripts/
